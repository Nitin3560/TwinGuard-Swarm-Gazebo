services:
  ros2-dev:
    image: osrf/ros:jazzy-desktop
    container_name: twinguard-swarm-ros2-dev
    network_mode: host
    stdin_open: true
    tty: true
    working_dir: /workspace
    volumes:
      - ./:/workspace
    command: bash
